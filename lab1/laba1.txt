dir = ls -0
ll = ls -alF

ls -thor = информация о файлах, которые лежат в данном каталоге 

!! - выводит последнюю введённую команду и лог её исполнения
!!:p - последня введённая команда 

cd . - "." указатель на текущую деректорию => ничего не произойдёт

cd <Tab><Tab> - предлагает список допустимых для перемещения каталогов 
cd /h<Tab><Tab> - возвращает в директорию "home", при повторой табуляции указывается имя текущего пользователя. 

tac - читает файл "снизу вверх"
less - имеет намного больше опций, чем more, например поиск слов в текстовом файле, так же подгружает файл постеменно, а не весь сразу 


